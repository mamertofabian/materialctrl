@model HomeIndexViewModel

@{
    ViewBag.Title = "Material Control";
}

<h1>All Projects</h1>

@foreach (var project in Model.Projects) {
    @Html.Partial("_Summary", project)
}

<div>
    <a class="btn btn-primary" asp-action="Create"><i class="fa fa-plus"></i> Add Project</a>
</div>
