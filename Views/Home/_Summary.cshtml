@model Project

<section>
    <h3>@Model.Name</h3>
    <div>
        <a asp-action="Details" asp-route-id="@Model.Id">More</a>
        <a asp-page="/Projects/Edit" asp-route-id="@Model.Id">Edit</a>
    </div>
</section>